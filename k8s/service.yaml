apiVersion: v1
kind: Service
metadata:
  name: netops-agent-platform
  namespace: netops-agents
spec:
  selector:
    app: netops-agent-platform
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
