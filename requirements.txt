fastapi>=0.133,<1
uvicorn[standard]>=0.34,<1
pydantic>=2.10,<3
numpy==2.2.1

google-adk==2.8.0

# optional: real Neo4j backend; without it the graph is in-memory.
neo4j==6.3.0

# Multi-tenancy, accounts and the audit trail (imported at startup in every mode).
sqlalchemy[asyncio]==2.0.54
asyncpg==0.31.0
alembic==1.20.0
psycopg2-binary==2.9.13
pydantic-settings==2.15.0
email-validator==2.3.0
python-jose[cryptography]==3.5.0
python-multipart==0.0.32

# bcrypt directly, not passlib: passlib 1.7.4 is unmaintained and breaks on bcrypt >= 4.1.
bcrypt==5.0.0

# Background agent runs. Without BROKER_URL, Celery runs tasks eagerly.
celery[redis]==5.6.3
redis==6.4.0

# GraphQL read surface, mounted at /graphql beside the REST API.
strawberry-graphql[fastapi]==0.327.7

# pytest-asyncio 1.4 requires pytest 9.
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.8.4
